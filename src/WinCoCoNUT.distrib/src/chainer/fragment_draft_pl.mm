# args=-l 100 -p -v -absolute -q NC_002745.fna.draft.shuffled /home/mibrahim/Myprojects/GenomeChainer/distChainerWithSourceLinux5/src/indexNC_003923
# matches are reported in the following way
# l(S) p(S) t l(Q) p(Q) d e s i
# where:
# l = length
# p = absolute position
# t = type (D=direct, P=palindromic)
# d = distance value (negative=hamming, 0=exact, positive=edit)
# e = E-value
# s = score value (negative=hamming, positive=edit)
# i = percent identity
# (S) = in Subject
# (Q) = in Query
